name: QA Workflow

# trigger
on: workflow_dispatch

jobs:
  build-version-8.3:
    name: "Build"
    uses: ./.github/workflows/build.yaml
    with:
      version: "8.3"

  build-version-8.2:
    name: "Build"
    uses: ./.github/workflows/build.yaml
    with:
      version: "8.2"
